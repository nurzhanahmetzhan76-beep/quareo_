# RetailPool AI v2.0 — API Server
FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY retailpool/ retailpool/

EXPOSE 8000

CMD ["uvicorn", "retailpool.main:app", "--host", "0.0.0.0", "--port", "8000"]
