FROM python:3.12-slim

WORKDIR /app

# Install only bot dependencies (no Playwright needed)
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 5000

CMD ["python", "run_bot.py", "--webhook"]
